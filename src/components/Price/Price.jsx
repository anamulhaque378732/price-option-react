import PropTypes from 'prop-types';

const Price = ({ price }) => {
    // console.log(price);

    return (
        <div>
            



        </div>
    );
};

Price.propTypes = {
    price: PropTypes.object.isRequired
}

export default Price;